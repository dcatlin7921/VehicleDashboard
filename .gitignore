# Dependencies
# Ignore dependency folders to avoid committing thousands of files.
# These should be installed by package managers (like npm or yarn) on each developer's machine.
/node_modules
/bower_components

# Logs
# Log files are typically machine-specific and not useful to other developers.
logs
*.log
npm-debug.log*
yarn-debug.log*

# Build & Test Output
# These files are generated during the build or test process and can be recreated at any time.
/coverage
/.nyc_output

# OS & Editor Files
# Ignore files generated by operating systems and code editors to keep the repo clean.
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp

# Environment Variables
# CRITICAL: Keep environment files out of version control to protect sensitive data like API keys.
.env
.env.test
